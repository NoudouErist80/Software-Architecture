all:
  hosts:
    vibe-server:
      ansible_host: "{{ oracle_vps_ip }}"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/vibe_oracle_key
  vars:
    ansible_python_interpreter: /usr/bin/python3
